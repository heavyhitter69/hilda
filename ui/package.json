{
  "name": "hilda-ui",
  "version": "1.0.0",
  "description": "Hilda — Dynamic Island desktop voice assistant",
  "author": "Hilda <sarkodieemil@gmail.com>",
  "homepage": "https://github.com/heavyhitter69/hilda",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder",
    "build:backend": "cd .. && pip install -r requirements.txt && python -m PyInstaller --noconfirm --clean hilda-engine.spec",
    "build:website": "cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --win --x64 --arm64 --publish never",
    "build:folder": "cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --win dir --publish never",
    "build:mac": "cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --mac --x64 --arm64 --publish never",
    "build:linux": "cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --linux --x64 --arm64 --publish never",
    "build:full": "npm run build:backend && npm run build:website && npm run build:mac && npm run build:linux"
  },
  "build": {
    "appId": "com.hilda.assistant",
    "productName": "Hilda",
    "artifactName": "Hilda-Setup-${os}-${arch}.${ext}",
    "icon": "build/icon.png",
    "directories": {
      "output": "../website/downloads"
    },
    "extraResources": [
      {
        "from": "../dist/hilda-engine",
        "to": "backend",
        "filter": ["**/*"]
      },
      {
        "from": "../.env.example",
        "to": "defaults"
      }
    ],
    "win": {
      "target": [
        "nsis",
        "nsis-web"
      ],
      "signAndEditExecutable": false
    },
    "mac": {
      "target": "dmg"
    },
    "linux": {
      "target": [
        "AppImage",
        "deb",
        "rpm"
      ],
      "category": "Utility"
    },
    "nsis": {
      "oneClick": true,
      "perMachine": true,
      "shortcutName": "Hilda"
    },
    "nsisWeb": {
      "oneClick": true,
      "perMachine": false,
      "artifactName": "Hilda-Setup-${os}-${arch}-User.${ext}",
      "shortcutName": "Hilda"
    },
    "files": [
      "**/*",
      "!node_modules/**/*"
    ]
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "electron": "^30.0.0",
    "electron-builder": "^24.0.0"
  },
  "dependencies": {
    "@dicebear/collection": "^9.4.0",
    "@dicebear/core": "^9.4.0"
  }
}
